
                                          🧭 Control Statements in Java

<h2 align="center"><b> ✅ 1. Decision Control Statements</b></h2>

* Used to make decisions based on conditions.

<h3 align="center"><b>🔹 if Statement</b> </h3>
* Executes code only if the condition is true.

```
if (condition) {
    // true block
}
```

<h3 align="center"><b>🔹 if...else </b> </h3>

* Choose between two blocks based on condition.
```
if (condition) {
    // true block
} else {
    // false block
}
```


<h3 align="center"><b>🔹 else if Ladder </b></h3>

* Checks multiple conditions in sequence.
```
if (cond1) {
    // block 1
} else if (cond2) {
    // block 2
} else {
    // default block
}
```

<h3 align="center"><b> 🔹 Ternary Operator   </b></h3>

* Shorthand for if...else.
```
(condition) ? value_if_true : value_if_false;
```



<h3 align="center"><b>🔹 switch-case </b></h3>

* Used when multiple conditions depend on a single expression.
```
switch(expression) {
    case constant1:
        // code
        break;
    case constant2:
        // code
        break;
    default:
        // default code
}
```


<h2 align="center"><b> 🔁 2. Iterative (Looping) Statements </b></h2>

* Used to execute a block of code repeatedly.

<h3 align="center"><b>🔹 for loop </b></h3>

```
for(initialization; condition; increment) {
    // loop body
}
```


<h3 align="center"><b>🔹 while loop </b></h3>

```
while(condition) {
    // loop body
}
```


<h3 align="center"><b> 🔹 do-while loop </b></h3>

```
do {
    // loop body
} while(condition);
```


<h2 align="center"><b> 🚀 3. Jumping Statements </b></h2> 

* Used to jump from one part of the code to another.

<h2 align="center"><b>🔹 break </b></h2>

<h4 align="center"><b>✅ break (without label) </b></h4>
<b>Syntax</b>

```
break;
```
<h5 align="center"><b> 📌 Used Inside </b></h5>

```
for (...) {
    if (condition) {
        break;
    }
}
```

<h4 align="center"><b>✅ break (with label) </b></h4>

* Stops a specific outer loop when loops are nested.

<b> Syntax </b>
```
labelName:
loop {
    break labelName;
}
```
<h4 align="center"><b> Real Structure (Nested Loop)</b></h4>

```
outer:
for (initialization; condition; increment) {
    for (initialization; condition; increment) {
        if (condition) {
            break outer;
        }
    }
}
```


<h3 align="center"><b> 🔹 continue </b></h3>
Skips the current iteration and moves to the next.


